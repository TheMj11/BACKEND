"use strict";

class MetricExporter {
  constructor(meterProviderName, options = {}) {
    this._meterProviderName = meterProviderName;
    this._aggregationTemporality = options?.aggregationTemporality;
    this._exportIntervalMillis = options?.exportIntervalMillis;
    this._shutdown = false;
  }

  async export(metrics, callback) {
    throw new Error("This method is not implemented!");
  }

  forceFlush() {
    return Promise.resolve();
  }

  selectAggregationTemporality() {
    return this._aggregationTemporality;
  }

  // TODO selectAggregation?(instrumentType: InstrumentType): Aggregation;

  shutdown() {
    this._shutdown = true;
    return Promise.resolve();
  }
}

module.exports = MetricExporter;
