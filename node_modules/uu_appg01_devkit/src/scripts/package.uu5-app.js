const helpers = require("../tools/helpers.js");

module.exports = class Package {
  constructor(config) {
    this.config = config;
  }
  async process() {
    return await helpers.forwardTaskToUu5Devkit("package", this.config);
  }
};
