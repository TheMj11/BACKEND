const { TestHelper } = require("uu_script_devkitg01");

describe("<%= context.className%><%= context.cmdName[0].toUpperCase() + context.cmdName.substring(1)%>", () => {
  test("HDS", async () => {
    const session = await TestHelper.login();

    const dtoIn = {};

    const result = await TestHelper.runScript("<%= context.entity%>/<%= context.scriptName%>.js", dtoIn, session);
    expect(result.isError).toEqual(false);
  });
});
