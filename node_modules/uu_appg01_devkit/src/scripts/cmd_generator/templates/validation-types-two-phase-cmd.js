const <%=  context.cmdValidationType%> = shape({
  mode: oneOf("standard", "retry", "rollback").isRequired(),
  data: shape({}, true).isRequired(),
  force: boolean(),
});

const <%=  context.finalizeCmdValidationType%> = shape({
  lockSecret: hexa64Code().isRequired(),
});

const <%=  context.finalizeRollbackCmdValidationType%> = shape({
  lockSecret: hexa64Code().isRequired(),
});
