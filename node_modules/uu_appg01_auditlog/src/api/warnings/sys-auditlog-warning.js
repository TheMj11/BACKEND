"use strict";
const Errors = require("../errors/sys-auditlog-error.js");

const createAuditLogRecord = {
  unsupportedKeys: {
    code: `${Errors.CreateAuditLogRecord.UC_CODE}unsupportedKeys`,
  },
};

const deleteBeforeTime = {
  unsupportedKeys: {
    code: `${Errors.DeleteBeforeTime.UC_CODE}unsupportedKeys`,
  },
};

const logRecordList = {
  unsupportedKeys: {
    code: `${Errors.LogRecordList.UC_CODE}unsupportedKeys`,
  },
};

const transfer = {
  transferHasAlreadyStarted: {
    code: `${Errors.LogRecordTransfer.UC_CODE}transferHasAlreadyStarted`,
    message: "The transfer has already started.",
  },
};

const logRecordPrivilegedList = {
  unsupportedKeys: {
    code: `${Errors.LogRecordPrivilegedList.UC_CODE}unsupportedKeys`,
  },
};

const privilegedDelete = {
  unsupportedKeys: {
    code: `${Errors.PrivilegedDelete.UC_CODE}unsupportedKeys`,
  },
};

const configRecordCreate = {
  configRecordWasCreated: {
    code: `${Errors.CreateAuditLogConfig.UC_CODE}unsupportedKeys`,
    message: "AuditLogConfig was already created.",
  },
};

module.exports = {
  createAuditLogRecord,
  deleteBeforeTime,
  logRecordList,
  transfer,
  logRecordPrivilegedList,
  privilegedDelete,
  configRecordCreate,
};
