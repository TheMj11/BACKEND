"use strict";
const <%= context.projectClassName %>UseCaseError = require("./<%= context.projectClassFile %>-use-case-error.js");

class ConfigurationLoadFailed extends <%= context.projectClassName %>UseCaseError {
  constructor(paramMap = {}, cause = null) {
    super("configurationLoadFailed", "Failed to obtain configuration needed to continue.", paramMap, cause);
  }
}

class UuAppMetaModelDoesNotExist extends <%= context.projectClassName %>UseCaseError {
  constructor(paramMap = {}, cause = null) {
    super("uuAppMetaModelDoesNotExist", "Metamodel does not exist.", paramMap, cause);
  }
}

module.exports = {
  ConfigurationLoadFailed,
  UuAppMetaModelDoesNotExist,
};
