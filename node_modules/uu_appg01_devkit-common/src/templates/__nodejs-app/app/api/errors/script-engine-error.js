"use strict";
const <%= context.projectClassName %>UseCaseError = require("./<%= context.projectClassFile %>-use-case-error.js");

class CallScriptEngineFailed extends <%= context.projectClassName %>UseCaseError {
  constructor(paramMap = {}, cause = null) {
    super("callScriptEngineFailed", "Call scriptEngine failed.", paramMap, cause);
  }
}

module.exports = {
  CallScriptEngineFailed,
};
