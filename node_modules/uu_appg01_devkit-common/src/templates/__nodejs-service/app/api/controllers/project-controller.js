"use strict";
const InitAbl = require("../../abl/<%= context.projectClassFile%>/init-abl.js");
const LoadAbl = require("../../abl/<%= context.projectClassFile%>/load-abl.js");

class <%= context.projectClassName %>Controller {
  init(ucEnv) {
    return InitAbl.init(ucEnv.getUri(), ucEnv.getDtoIn(), ucEnv.getSession());
  }

  load(ucEnv) {
    return LoadAbl.load(ucEnv.getUri(), ucEnv.getSession());
  }

  loadBasicData(ucEnv) {
    return LoadAbl.loadBasicData(ucEnv.getUri(), ucEnv.getSession());
  }
}

module.exports = new <%= context.projectClassName %>Controller();
