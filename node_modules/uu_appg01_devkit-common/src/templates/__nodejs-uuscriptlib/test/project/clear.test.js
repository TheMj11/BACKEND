const { TestHelper } = require("uu_script_devkitg01");

describe("<%= context.projectClassName %>Clear", () => {
  test("HDS", async () => {
    const session = await TestHelper.login();

    const dtoIn = {};

    const result = await TestHelper.runScript("<%= context.projectClassFile %>/clear.js", dtoIn, session);
    expect(result.isError).toEqual(false);
  });
});
